<ScheduleScreen>:
    BoxLayout:
        orientation: 'vertical'
        spacing: dp(20)
        padding: dp(20)

        Label:
            text: "Minhas Escalas"
            font_size: dp(24)

        ScrollView:
            Label:
                id: escalas_lista
                text: "Carregando suas escalas..."
                size_hint_y: None
                height: self.texture_size[1]

        Button:
            text: "Voltar"
            on_press: root.manager.current = "dashboard"
